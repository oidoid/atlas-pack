{
  "scripts": {
    "test:unit": "jest",
    "test": "npm -s run formatter -- -l && npm -s run build:demo && npm -s run build:lib && npm -s run test:unit",
    "build:demo": "cd demo && npm -s run build",
    "build:lib": "cd lib && npm -s run build",
    "format": "npm -s run formatter -- -w",
    "formatter": "prettier './**/*.{js,json,html,md,ts}'"
  },
  "workspaces": [
    "lib",
    "demo"
  ],
  "engines": {
    "node": "15.2.1",
    "npm": "7.0.8"
  },
  "devDependencies": {
    "clean-webpack-plugin": "3.0.0",
    "jest": "26.6.3",
    "pre-commit": "1.2.2",
    "prettier": "2.2.0",
    "ts-jest": "26.4.4",
    "ts-loader": "8.0.11",
    "typescript": "4.1.2",
    "webpack": "5.8.0",
    "webpack-cli": "4.2.0"
  }
}
