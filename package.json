{
  "scripts": {
    "test:unit": "jest",
    "test": "npm -s run formatter -- -l && npm -s run build:lib && npm -s run build:demo && npm -s run test:unit",
    "build:demo": "cd demo && npm -s run build",
    "build:lib": "cd lib && npm -s run build",
    "format": "npm -s run formatter -- -w",
    "formatter": "prettier './**/*.{js,json,html,md,ts}'"
  },
  "workspaces": [
    "lib",
    "demo"
  ],
  "engines": {
    "node": "15.8.0",
    "npm": "7.5.1"
  },
  "devDependencies": {
    "clean-webpack-plugin": "3.0.0",
    "jest": "26.6.3",
    "pre-commit": "1.2.2",
    "prettier": "2.2.1",
    "ts-jest": "26.5.0",
    "ts-loader": "8.0.15",
    "typescript": "4.2.0-beta",
    "webpack": "5.21.1",
    "webpack-cli": "4.5.0"
  },
  "name": "aseprite-atlas"
}
